This is a proof-of-concept of fast atmosphere simulator. It can be built with
decent C compiler.

To run it either hardcode desired terminal size in `main` or, on Linux, export
COLUMNS and LINES variables.
